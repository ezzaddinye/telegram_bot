# دليل استخدام بوت تحليل طلبات المساعدة الطلابية

## 📖 المقدمة

هذا الدليل الشامل سيساعدك على إعداد وتشغيل البوت بنجاح لتحليل طلبات المساعدة الطلابية من الجروبات.

## 🎯 ما يفعله البوت

البوت يقوم بالخطوات التالية بشكل تلقائي:

1. **🔍 المراقبة المستمرة**: يراقب جميع الرسائل في الجروبات التي أنت مشترك فيها
2. **📊 التحليل الذكي**: يحلل محتوى كل رسالة للتعرف على طلبات المساعدة
3. **🎯 التصفية**: يحدد نوع الخدمة المطلوبة (شرح، تقارير، واجبات، إلخ)
4. **📤 النقل**: يحول الطلبات المكتشفة إلى جروب خاص تحدده أنت
5. **🔗 الروابط**: يرفق روابط سهلة للوصول للرسالة الأصلية والمستخدم

## 🚀 خطوات التثبيت والإعداد

### الخطوة 1: تحضير المتطلبات

#### على Windows:
1. تأكد من تثبيت Python 3.7 أو أحدث
   - تحميل من: https://www.python.org/downloads/
   - أثناء التثبيت: اختر "Add Python to PATH"

#### على Linux/Mac:
```bash
sudo apt update
sudo apt install python3 python3-pip -y
```

### الخطوة 2: تحميل المشروع

```bash
git clone <رابط_المشروع>
cd student-help-bot
```

### الخطوة 3: تثبيت المكتبات

```bash
pip install -r requirements.txt
```

### الخطوة 4: الحصول على بيانات API

#### أولاً: الحصول على API_ID و API_HASH

1. افتح المتصفح واذهب إلى: https://my.telegram.org
2. سجل الدخول برقم هاتفك المسجل في تيليجرام
3. انتظر رمز التحقق واكتبه
4. من القائمة اختر **API development tools**
5. املأ النموذج:
   - **App title**: أي اسم (مثلاً: Student Help Bot)
   - **Short name**: اسم قصير (مثلاً: studentbot)
   - **Platform**: اختر Desktop
   - **Description**: اتركه فارغاً أو اكتب وصفاً قصيراً
6. اضغط **Create application**
7. ستحصل على:
   - `api_id`: رقم (مثلاً: 12345678)
   - `api_hash`: نص طويل (مثلاً: a1b2c3d4e5f6...)

💾 **انسخ هذين الرقمين واحتفظ بهما**

#### ثانياً: الحصول على Target Group ID

1. أنشئ جروب جديداً في تيليجرام (سيكون هذا الجروب لاستقبال الطلبات)
2. أضف البوت `@userinfobot` إلى هذا الجروب
3. في الجروب، اكتب `/start`
4. سيرد عليك البوت بمعلومات، ابحث عن **Chat Id** أو **ID**
5. النسيق سيكون مثل: `-1001234567890` (القيمة السالبة هي الصحيحة)

💾 **انسخ هذا الرقم**

### الخطوة 5: إعداد ملف `.env`

1. افتح مجلد المشروع
2. ابحث عن ملف `.env.example`
3. أنشئ نسخة منه باسم `.env`
   - **على Windows**: أعد تسمية الملف من `.env.example` إلى `.env`
   - **على Linux/Mac**: `cp .env.example .env`

4. افتح الملف `.env` بالمحرر النصي:
   - **Windows**: Notepad أو Notepad++
   - **Linux/Mac**: `nano .env` أو `vi .env`

5. عدّل البيانات التالية:

```env
# API_ID من my.telegram.org (بدون علامات تنصيص)
API_ID=12345678

# API_HASH من my.telegram.org (كما هو)
API_HASH=a1b2c3d4e5f6...

# رقم هاتفك مع رمز الدولة (بدون علامات تنصيص)
# مثال: +966500000000
PHONE=+966500000000

# معرف الجروب الخاص (القيمة السالبة كاملة)
TARGET_GROUP_ID=-1001234567890

# الخيارات المتقدمة (يمكن تركها كما هي)
MIN_DELAY=0.5
MAX_DELAY=3.0
ENABLE_LOGGING=true
```

⚠️ **ملاحظات مهمة:**
- لا تضع علامات تنصيص حول القيم
- تأكد من كتابة رقم الهاتف مع رمز الدولة (+966 للسعودية، +966 للكويت، إلخ)
- تأكد من استخدام القيمة السالبة الكاملة لمعرف الجروب
- لا تترك مسافات حول علامة `=`

## 🎮 تشغيل البوت

### على Windows:

#### الطريقة 1: باستخدام ملف الباتش (سهلة)
1. انقر مرتين على `start.bat`
2. سيبدأ البوت تلقائياً

#### الطريقة 2: يدوياً
1. افتح موجه الأوامر (CMD) أو PowerShell
2. انتقل لمجلد المشروع:
   ```cmd
   cd path\to\student-help-bot
   ```
3. شغل البوت:
   ```cmd
   python student_help_bot.py
   ```

### على Linux/Mac:

#### الطريقة 1: باستخدام سكريبت bash (سهلة)
```bash
./start.sh
```

#### الطريقة 2: يدوياً
```bash
python3 student_help_bot.py
```

### أول تشغيل (التفعيل)

في المرة الأولى التي تشغل فيها البوت، سيحدث التالي:

```
🚀 بدء تشغيل البوت بنجاح!

Please enter the phone number you want to use: +966500000000

Please enter the code you received: 12345

Please enter your password (optional): [اضغط Enter إذا لم يكن لديك]
```

1. **رقم الهاتف**: أدخل رقم هاتفك مع رمز الدولة (مثلاً: +966500000000)
2. **رمز التحقق**: ستصل رسالة إلى تيليجرام، أدخل الرمز
3. **كلمة المرور**: إذا كان لديك التحقق بخطوتين، أدخلها، وإلا اضغط Enter

✅ بعد ذلك سيبدأ البوت بالعمل تلقائياً!

## 📱 من الجروبات التي يراقبها البوت؟

البوت يراقب **جميع الجروبات** التي أنت مشترك فيها بشكل افتراضي.

### تقييد الجروبات (اختياري)

إذا أردت تقييد البوت على جروبات معينة، يمكنك تعديل الكود:

1. افتح ملف `student_help_bot.py`
2. ابحث عن:
   ```python
   @events.NewMessage(incoming=True)
   ```
3. عدله ليشمل الجروبات المحددة:
   ```python
   @events.NewMessage(incoming=True, chats=[GROUP_ID_1, GROUP_ID_2])
   ```

للحصول على معرفات الجروبات:
- استخدم `@userinfobot` في كل جروب
- سيعطيك معرف الجروب

## 🔍 اختبار البوت

### كيف تتأكد أن البوت يعمل؟

1. **راقب السجلات**: البوت يعرض نشاطه في الشاشة
   ```
   🤖 بدء تشغيل البوت...
   👀 البوت جاهز للمراقبة والتحليل...
   🕐 تأخير عشوائي: 1.23 ثانية
   📊 تم العثور على 5 جروب للمراقبة
   ```

2. **أرسل رسالة تجريبية**:
   - في أي جروب، اكتب: "محتاج مساعدة في واجب الرياضيات"
   - انتظر بضع ثوانٍ
   - اذهب للجروب الخاص
   - يجب أن تجد الرسالة منقولة هناك

### رسالة نموذجية منقولة:

```
🎯 اكتشاف طلب مساعدة جديد!

📝 الخدمة المطلوبة: واجبات

👤 المرسل: @username

📄 نص الرسالة الأصلية:
محتاج مساعدة في واجب الرياضيات

🔗 الروابط:
• الانتقال للرسالة الأصلية: https://t.me/c/123456/789
• دردشة مباشرة: https://t.me/username

⏰ وقت الاكتشاف: 2024-01-15 14:30:25
```

## 🌧️ تشغيل البوت على الخادم السحابي (VPS)

### لماذا نستخدم VPS؟

✅ يعمل 24/7 دون انقطاع
✅ اتصال إنترنت مستقر
✅ يمكنك التحكم من جوالك
✅ لا يحتاج لجهازك أن يكون مفتوحاً

### خطوات النشر على DigitalOcean (مثال):

#### 1. إنشاء حساب وشراء خادم
1. سجل في: https://www.digitalocean.com
2. اختر **Create Droplet**
3. اختر:
   - **Image**: Ubuntu 20.04 LTS
   - **Plan**: Basic $5/ماهر (كافٍ للبوت)
   - **Authentication**: Password (أو SSH Key إذا كنت متقدماً)
4. اختر **Create Droplet**

#### 2. الاتصال بالخادم

من جهازك:

```bash
ssh root@server_ip_address
```

أدخل كلمة المرور التي أرسلوها لك

#### 3. إعداد الخادم

```bash
# تحديث النظام
apt update && apt upgrade -y

# تثبيت Python
apt install python3 python3-pip git -y

# تحميل المشروع
git clone <رابط_المشروع>
cd student-help-bot

# تثبيت المكتبات
pip3 install -r requirements.txt

# إنشاء ملف .env
nano .env
```

انسخ البيانات من ملف `.env` عندك والصقها

#### 4. تشغيل البوت في الخلفية

**باستخدام screen:**
```bash
# تثبيت screen
apt install screen -y

# إنشاء جلسة جديدة
screen -S help_bot

# تشغيل البوت
python3 student_help_bot.py

# للخروج من screen (البوت يستمر بالعمل)
# اضغط: Ctrl + A ثم D

# للعودة للبوت
screen -r help_bot
```

**باستخدام tmux:**
```bash
# تثبيت tmux
apt install tmux -y

# إنشاء جلسة جديدة
tmux new -s help_bot

# تشغيل البوت
python3 student_help_bot.py

# للخروج: Ctrl + B ثم D
# للعودة: tmux attach -t help_bot
```

### مراقبة البوت من الجوال

#### 1. رؤية نشاط البوت:
- افتح تطبيق تيليجرام على جوالك
- سترى كل ما يفعله البوت
- ستصل الرسائل المنقولة للجروب الخاص

#### 2. التحكم بالخادم:
- حمل تطبيق **Termius** أو أي تطبيق SSH
- أضف بيانات الخادم:
  - **Host**: عنوان IP للخادم
  - **Port**: 22
  - **Username**: root
  - **Password**: كلمة مرور الخادم
- اتصل بالخادم وتحكم بالبوت

## 🔧 الصيانة

### عرض السجلات:
```bash
tail -f bot.log
```

### إعادة تشغيل البوت:

**إذا كان يعمل في screen:**
```bash
screen -r help_bot
# اضغط Ctrl + C لإيقافه
python3 student_help_bot.py
# ثم Ctrl + A ثم D للخروج
```

**إذا كان يعمل في tmux:**
```bash
tmux attach -t help_bot
# اضغط Ctrl + C لإيقافه
python3 student_help_bot.py
# ثم Ctrl + B ثم D للخروج
```

### تحديث البوت:
```bash
cd /root/student-help-bot
git pull
pip3 install -r requirements.txt --upgrade
# ثم أعد التشغيل
```

## ⚠️ تحذيرات ونصائح

### لماذا قد يُحظر حسابك؟

❌ إرسال رسائل بسرعة مفرطة
❌ الانضمام لعدد كبير جداً من الجروبات
❌ استخدام البوت في الجروبات المحظورة
❌ الإبلاغ عنك كسبام من قبل المستخدمين

### كيف تتجنب الحظر؟

✅ البوت يستخدم تأخيرات عشوائية تلقائياً
✅ لا يرسل رسائل، فقط يقرأ وينقل
✅ يتعامل مع أخطاء FloodWait تلقائياً
✅ يستخدم إعدادات آمنة افتراضياً

### نصائح إضافية:

1. **ابدأ بعدد قليل من الجروبات**: 5-10 جروبات في البداية
2. **راقب السجلات بانتظام**: للتأكد من عدم وجود أخطاء
3. **استخدم جروب خاص محمي**: للرسائل المنقولة
4. **لا تشارك الكود مع أشخاص غير موثوقين**: يحتوي على بياناتك

## 🆘 حل المشاكل الشائعة

### المشكلة: "API_ID مطلوب"
**الحل**: تأكد من إدخال `API_ID` في ملف `.env` بشكل صحيح

### المشكلة: "TARGET_GROUP_ID مطلوب"
**الحل**: 
1. تأكد من وجود معرف الجروب في `.env`
2. تأكد من استخدام القيمة السالبة كاملة

### المشكلة: " FloodWaitError"
**الحل**: البوت يتعامل معه تلقائياً، انتظر بضع دقائق

### المشكلة: البوت لا يرسل رسائل
**الحل**:
1. تأكد من أن البوت عضو في الجروب الخاص
2. تأكد من أن الجروب ليس مقيدًا
3. راجع سجلات `bot.log`

### المشكلة: البوت لا يكتشف الرسائل
**الحل**:
1. تأكد أن الرسالة تحتوي على كلمات مفتاحية
2. تأكد أن طول الرسالة أكثر من 10 حروف
3. راجع سجلات البوت

## 📞 الدعم والمساعدة

إذا واجهت مشاكل:

1. راجع سجلات `bot.log`
2. تأكد من صحة بيانات `.env`
3. تأكد من اتصال الإنترنت
4. اقرأ ملف `README.md` الرئيسي

## 🎉 الخاتمة

مبروك! أنت الآن جاهز لاستخدام البوت وتحليل طلبات المساعدة الطلابية.

تذكر دائماً:
- ✅ استخدم البوت بشكل مسؤول
- ✅ احترم خصوصية المستخدمين
- ✅ راقب أداء البوت بانتظام
- ✅ حدّث البوت بانتظام

**حظاً موفقاً! 🚀**